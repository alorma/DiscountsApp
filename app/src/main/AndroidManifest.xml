<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.alorma.discounts">

    <uses-feature android:name="android.hardware.camera" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning"
            android:name=".DiscountsApplication">

        <activity android:name=".ui.discountslist.DiscountsListActivity"
                  android:theme="@style/AppTheme.DiscountsList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.newdiscount.NewDiscountActivity"/>
        <activity android:name=".ui.discountdetail.DiscountDetailActivity"/>
        <activity android:name=".ui.barcode.BarcodeCaptureActivity"/>
    </application>

</manifest>